jobs:
- job: Skia_Desktop
  timeoutInMinutes: 90
  pool:
    vmImage: windows-2022

  steps:
  - checkout: self
    clean: true
    fetchDepth: 0
    persistCredentials: true


  - template: templates/canary-updater.yml
  - template: templates/gitversion.yml
  - template: templates/dotnet-install-windows.yml

  - task: MSBuild@1
    displayName: Build project
    retryCountOnTaskFailure: 3
    inputs:
      solution: src\Chefs\Chefs.csproj
      msbuildArguments: /ds /m /r /p:TargetFramework=net9.0-desktop /p:TargetFrameworkOverride=net9.0-desktop /p:Configuration=Release "/p:InformationalVersion=%GITVERSION_InformationalVersion%" /bl:$(build.artifactstagingdirectory)/build-desktop.binlog

  - task: CopyFiles@2
    displayName: Copy Skia output
    inputs:
      SourceFolder: src\Chefs\bin\Release\net9.0-desktop
      Contents: '**/*.*'
      TargetFolder: $(build.artifactstagingdirectory)/bin

  - task: PublishBuildArtifacts@1
    condition: always()
    inputs:
      PathtoPublish: $(build.artifactstagingdirectory)
      ArtifactName: skia-desktop
      ArtifactType: Container