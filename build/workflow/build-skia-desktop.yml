jobs:
- job: Skia_GTK
  timeoutInMinutes: 90
  pool:
    vmImage: windows-2022

  steps:
  - template: templates/canary-updater.yml
  - template: templates/gitversion.yml
  - template: templates/dotnet-install-windows.yml

  - task: MSBuild@1
    displayName: Build project
    retryCountOnTaskFailure: 3
    inputs:
      solution: src\Chefs\Chefs.csproj
      msbuildArguments: /ds /m /r /p:TargetFramework=net8.0 /p:Configuration=Release "/p:InformationalVersion=%GITVERSION_InformationalVersion%" /bl:$(build.artifactstagingdirectory)/build-desktop.binlog

  - task: CopyFiles@2
    displayName: Copy Skia output
    inputs:
      SourceFolder: src\Chefs\bin\Release\net8.0
      Contents: '**/*.*'
      TargetFolder: $(build.artifactstagingdirectory)/bin

  - task: PublishBuildArtifacts@1
    condition: always()
    inputs:
      PathtoPublish: $(build.artifactstagingdirectory)
      ArtifactName: skia-gtk
      ArtifactType: Container