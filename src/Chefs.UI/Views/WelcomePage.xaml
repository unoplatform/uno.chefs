<Page x:Class="Chefs.Views.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Chefs.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      xmlns:msui="using:Microsoft.UI.Xaml.Controls"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <utu:AutoLayout Background="{ThemeResource BackgroundBrush}">
        <utu:AutoLayout utu:AutoLayout.PrimaryAlignment="Stretch">
            <Image Source="https://picsum.photos/390/340"
                   Stretch="UniformToFill"
                   utu:AutoLayout.PrimaryLength="340"
                   Margin="0,0,0,-40" />
            <Grid Background="{ThemeResource SurfaceBrush}"
                  Padding="60,40"
                  utu:AutoLayout.PrimaryAlignment="Stretch"
                  CornerRadius="40"
                  RowSpacing="35">
                <Grid.RowDefinitions>
                    <RowDefinition Height="56" />
                    <RowDefinition Height="96" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="56" />
                    <RowDefinition Height="56" />
                </Grid.RowDefinitions>
                <Image Source="https://picsum.photos/189/57"
                       Stretch="Uniform"
                       utu:AutoLayout.CounterAlignment="Center"
                       utu:AutoLayout.PrimaryLength="56"
                       utu:AutoLayout.CounterLength="189"
                       Grid.Row="0" />
                <FlipView VerticalAlignment="Top"
                          Background="Transparent"
                          Grid.Row="1">
                    <FlipView.Items>
                        <FlipViewItem>
                            <TextBlock Foreground="{ThemeResource SurfaceInverseBrush}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Text="Easy recipes for busy devs.&#xA;UNO Platform and UXDivers joined forces &#xA;to create this beautyful inspiring demo app. &#xA;Enjoy it."
                                       utu:AutoLayout.CounterAlignment="Center" />
                        </FlipViewItem>
                        <FlipViewItem>
                            <TextBlock Foreground="{ThemeResource SurfaceInverseBrush}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Text="Easy recipes for busy devs.&#xA;UNO Platform and UXDivers joined forces &#xA;to create this beautyful inspiring demo app. &#xA;Enjoy it."
                                       utu:AutoLayout.CounterAlignment="Center" />
                        </FlipViewItem>
                        <FlipViewItem>
                            <TextBlock Foreground="{ThemeResource SurfaceInverseBrush}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Text="Easy recipes for busy devs.&#xA;UNO Platform and UXDivers joined forces &#xA;to create this beautyful inspiring demo app. &#xA;Enjoy it."
                                       utu:AutoLayout.CounterAlignment="Center" />
                        </FlipViewItem>
                    </FlipView.Items>
                </FlipView>
                <!--<TextBlock Foreground="{ThemeResource SurfaceInverseBrush}"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Text="Easy recipes for busy devs.&#xA;UNO Platform and UXDivers joined forces &#xA;to create this beautyful inspiring demo app. &#xA;Enjoy it."
                           utu:AutoLayout.CounterAlignment="Center" />-->
                <utu:AutoLayout Spacing="4"
                                PrimaryAxisAlignment="Center"
                                Orientation="Horizontal"
                                Grid.Row="2">
                    <Ellipse Fill="Red"
                             Width="8"
                             Height="8"
                             utu:AutoLayout.CounterAlignment="Center"
                             utu:AutoLayout.PrimaryLength="8"
                             utu:AutoLayout.CounterLength="8" />
                    <Ellipse Fill="{ThemeResource SurfaceVariantBrush}"
                             Width="8"
                             Height="8"
                             utu:AutoLayout.CounterAlignment="Center"
                             utu:AutoLayout.PrimaryLength="8"
                             utu:AutoLayout.CounterLength="8" />
                    <Ellipse Fill="{ThemeResource SurfaceVariantBrush}"
                             Width="8"
                             Height="8"
                             utu:AutoLayout.CounterAlignment="Center"
                             utu:AutoLayout.PrimaryLength="8"
                             utu:AutoLayout.CounterLength="8" />
                </utu:AutoLayout>
                <Button Background="{ThemeResource PrimaryBrush}"
                        Content="Next"
                        utu:AutoLayout.PrimaryLength="56"
                        Grid.Row="3"
                        HorizontalAlignment="Stretch"
                        Height="56" />
                <HyperlinkButton Content="Skip"
                                 Command="{Binding GoToLogin}"
                                 Grid.Row="4"
                                 HorizontalAlignment="Center"
                                 FontWeight="SemiBold" />
            </Grid>
        </utu:AutoLayout>
    </utu:AutoLayout>
</Page>
